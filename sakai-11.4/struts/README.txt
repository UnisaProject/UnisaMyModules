This is Unisa struts project.

We are currently experimenting with CKeditor so you'll have to add a property to sakai.properties called:
struts.editor.

So the property should look like this

struts.editor=FCKeditor;
